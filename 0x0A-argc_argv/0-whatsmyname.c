#include "main.h"
#include <stdio.h>

/**
 * main - Prints its name
 * @argv: argument vector
 * @argc: argument count
 *
 * Return: Always zero
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
